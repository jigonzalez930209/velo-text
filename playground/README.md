# Playground moved

The interactive demo is part of VitePress:

```bash
pnpm docs:dev
```

Open `/playground/`. Source: `docs/.vitepress/playground/` and `docs/.vitepress/theme/PlaygroundApp.vue`.

This directory is unused. You can delete leftover `node_modules/` / `dist/` here after `pnpm install` at the repo root.
