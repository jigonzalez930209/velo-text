# Core — Model

`src/core/model/types.ts` — `PortableDocument`, `BlockNode`, `InlineNode`, `AssetRef`, `PageSettings`, `Selection`, ports.

`src/core/model/factories.ts` — `createDocument(opts)`, `createParagraph`, `createHeading`, `createText`, `createVariable`, `createEquation`, `createImageBlock`, `createTable` with injected `IdGenerator`/`Clock`, defaults `A4 210x297mm`, `locale es-AR`.

See `src/core/model/*`.
